- >@Switch block is inspired by javascript switch statements.
- >Sample code:
```code
@switch (condition) {
  @case (caseA) {
    Case A.
  }
  @case (caseB) {
    Case B.
  }
  @default {
    Default case.
  }
}
```
- >@default block is compulsary at last of switch case.